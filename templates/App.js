"// React app is intentionally a no-op. The site is rendered from static HTML files
// in /app/frontend/public/ (index.html, login.html, signup.html, dashboard.html).
export default function App() {
  return null;
}
